rule all:
    input:
        "input"

rule viewWorkflow:

rule addMetaData:
    input:
        "input"
    output:
        "output"
    shell:
        "python3 -c 'import processFile as pf; pf.processFile({input},{output})'"

rule filterBenign:

rule makeReport:
